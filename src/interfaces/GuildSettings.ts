export interface GuildSettings {
    [key: string]: string;
    prefix: string;
    supportRole: string;
    embedColor: string;
    supportMsgChannel: string;
    adminChannel: string;
    supportMsg: string;
}
